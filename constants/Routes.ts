export enum Routes {
  LOGIN = '/(auth)/login',
  TABS = '/(tabs)',
  HOME = '/', // Root of the (tabs) group
  TRACKER = '/tracker',
  PROFILE = '/profile',
  SETTINGS = '/settings',
  // New route for the interactive meal entry
  MEAL_DETAILS = '/meal-details', 
}